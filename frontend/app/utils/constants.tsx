const apiBasePath = process.env.VERCEL_URL
  ? `https://${process.env.VERCEL_URL}`
  : `http://localhost:3001`;
//export const apiBaseUrl = `${apiBasePath}/api`;
export const apiBaseUrl = `chat-langchainjs-frontend-7j5u-q4d9cuhtm-dymock-bs-projects.vercel.app/api`;
